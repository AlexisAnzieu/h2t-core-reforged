# Migration `20201018215333`

This migration has been generated by alexis anzieu at 10/18/2020, 5:53:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `User` (
`id` int  NOT NULL  AUTO_INCREMENT,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL ,
`password` varchar(191)  NOT NULL ,
`loggedCount` int  NOT NULL ,
`email` varchar(191)  NOT NULL ,
`facebookUrl` varchar(191)  NOT NULL ,
`firstName` varchar(191)  ,
`lastName` varchar(191)  ,
`birthday` datetime(3)  ,
`picture` varchar(191)  ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Poem` (
`id` int  NOT NULL  AUTO_INCREMENT,
`authorId` int  ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL ,
`title` varchar(191)  NOT NULL ,
`description` varchar(191)  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Ad` (
`id` int  NOT NULL  AUTO_INCREMENT,
`authorId` int  ,
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updatedAt` datetime(3)  NOT NULL ,
`title` varchar(191)  NOT NULL ,
`description` varchar(191)  NOT NULL ,
`zipCode` varchar(191)  NOT NULL ,
`categories` ENUM('AUTRE', 'CUISINE', 'ELECTRONIQUE', 'MAISON', 'SERVICE', 'VETEMENT')  NOT NULL DEFAULT 'AUTRE',
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Poem` ADD FOREIGN KEY (`authorId`) REFERENCES `h2t`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `Ad` ADD FOREIGN KEY (`authorId`) REFERENCES `h2t`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201018212232..20201018215333
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model Event {
   id   Int @id @default(autoincrement())
@@ -16,5 +16,52 @@
   date String
   description String
   image String
   locations String
+}
+
+model User {
+  id Int @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  password String
+  loggedCount Int
+  email String
+  facebookUrl String
+  firstName String?
+  lastName String?
+  birthday DateTime?
+  picture String?
+  poems Poem[]
+  ads Ad[]
+}
+
+model Poem {
+  id Int @id @default(autoincrement())
+  author User?  @relation(fields: [authorId], references: [id])
+  authorId Int?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  title String
+  description String
+}
+
+model Ad {
+  id Int @id @default(autoincrement())
+  author User?  @relation(fields: [authorId], references: [id])
+  authorId Int?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  title String
+  description String
+  zipCode String
+  categories AdCategory @default(AUTRE)
+}
+
+enum AdCategory {
+  AUTRE
+  CUISINE
+  ELECTRONIQUE
+  MAISON
+  SERVICE
+  VETEMENT
 }
```


